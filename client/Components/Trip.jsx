import React from "react";
import { Link } from "react-router-dom";
import { useSelector } from "react-redux";


const Trip= (props) => {
  
  return (
    <>
        {`${props.NAME} + " - hello"`}
    </>
  );
};

export default Trip;